const fruits = ["Apple", "Banana", "Cherry"];
fruits.pop();

// Index number is always starts from zero so for adding third element as orange we have to write index number as 2 Not three.
fruits[2] = "Orange";
console.log(fruits);
